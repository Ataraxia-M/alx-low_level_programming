#include "main.h"

/**
* mul - multiplies two integers
* 
* @a: integer type parameter
* @b: interger type parameter
* 
* Returns: product of the two parameters
*/

int mul(int a, int b)
{
	return (a * b);
}